name: Compound A

on:
  workflow_call:
    inputs:
      sample:
        required: true
        type: string

jobs:
  compound-a:
    uses: ./.github/workflows/compound-a-component-0.yml
    with:
      sample: ${{ inputs.sample }}
