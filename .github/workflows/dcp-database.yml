name: "DCP: Database"

on:
  workflow_call:
    inputs:
      sample:
        required: true
        type: string

jobs:
  dcp-database:
    name: "DCP: Database"
    runs-on: ubuntu-latest
    steps:
      - name: Step 0
        run: |
          echo "sample: ${{ inputs.sample }}"
