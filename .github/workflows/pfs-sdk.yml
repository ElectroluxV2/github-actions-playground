name: "PFS: SDK"

on:
  workflow_call:
    inputs:
      sample:
        required: true
        type: string

jobs:
  sdk:
    runs-on: ubuntu-latest
    steps:
      - name: Step 0
        run: |
          echo "sample: ${{ inputs.sample }}"
